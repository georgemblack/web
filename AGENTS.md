# Agents

## Pull Requests

When opening a pull request:

- **Title**: Prefix the title with `AI:` (e.g., `AI: Add new feature`)
- **Description**: Include a note indicating the change was AI generated (e.g., `This change was AI generated.`)
